# 更新日志

## v1.5（2026-09-25）

**工作台完整代码同步；项目 → 需求 → 任务三层**

- `workbench/` 从首发快照同步到最新：1.1–1.4 以参考实现发布的任务抽取与飞书通知、说话人补标、
  术语词典，现在都是工作台里真实运行的代码（`notify.py`、`tasks.py`、`speaker_backfill.py`、
  `glossary.py`），外加 `workbench/card_listener.py` 回调监听。
- 项目 → 需求 → 任务三层（schema v12）：项目挂材料根目录，需求挂材料文件夹、关联会议，优先级
  P0–P3 只在需求上；任务项目跟随需求，改动写入讨论轨迹。材料浏览限制在 `MATERIAL_BROWSE_ROOT` 之下。
- 会议自动归属项目：AI 高置信匹配 → 已归属任务的多数项目 → 留空；人工改过即不再自动改。
- 待确认闸门减负：草稿 7 天自动过期（可恢复）、服务端计数与分页、批量驳回与撤销、抽取收窄。
- 资料库「需要处理」：失败与隔离的录音写清原因和下一步，旧失败可确认归档。
- 任务页行内确认 / 驳回 / 完成 / 取消；术语词典管理界面，范围与项目打通。
- 纪要卡发全文，超长按段切成多张卡；新增自建应用直连通道（不经 lark-cli）。
- 前端换成深色视觉体系，配色全部走 `:root` token。
- 修复：SQLite 连接显式关闭（句柄泄漏导致周期性宕机）；manifest 登记的纪要不再按文件名关键词
  排除（标题撞词被误隔离）；重复确认/驳回不再重复写入，撤销不能打回旧任务；界面不再露内部状态码。

## v1.4（2026-08-20）

**术语词典库：给纪要生成注入权威写法对照表**

- 会议纪要错字有两个来源，这一版收掉后半段——模型读转写稿写纪要时靠内部知识猜测产生的错字。
  建术语词典库，纪要生成时注入「正确写法 + 常见错写」对照表，模型按表纠错；同音字靠上下文
  纠，不靠热词（热词对同音字无解、多塞会假注入）。
- 词典靠「编辑纪要 diff 捕获更正」自动积累：对比新旧 markdown，捕获疑似错字更正进待确认队列，
  人工确认后错写并进正确写法的别名，越用越准。
- 快照契约：词典事实源在应用侧 SQLite，导 JSON 快照给转写侧只读注入，两进程靠文件契约对齐、
  不共享 schema。
- 排序服务注入上限：注入有 50 条上限，排序「人名 > 中文 > 英文缩写」，避免几乎不会写错的英文
  缩写占名额、把人名术语挤出注入表。
- 项目词按「转写稿命中强特征词（长度 ≥3）」判定注入，过滤「代表/专家/看板」这类 2 字高频
  通用词，避免无关项目词挤进注入表。
- 新增功能文档：`docs/glossary-correction.md`（五个设计决策 + 两层分域）。
- 新增参考实现：`glossary/snapshot_export.py`（快照排序与原子写）、
  `glossary/diff_corrections.py`（diff 更正提取与项目匹配）。

**关键设计决策**：错字根因拆成转写与纪要两环节、词典治后半段；词典价值在积累闭环不在建表；
排序要服务注入上限不是字母序；项目匹配用强特征词、误命中的代价大于漏命中。

## v1.3（2026-08-18）

**说话人分离：把引擎早就产出的数据接进逐字稿**

- 逐字稿每段显示说话人标签，改名界面把「说话人 1」标成真名；存量会议一次性回填，新会议
  导入时自动补标。断点只在导入器的来源优先级——引擎产出的说话人数据一直在磁盘上没被消费。
- 修复切块转写的说话人静默合并 bug：长录音分块后每块编号各自从 0 起，直接解析会把不同块的
  同号说话人并成一个标签（实测 14 场长会 195 个真实簇塌成 112 个）。修法是多块时标签带块
  前缀，解析层保真、跨块归并留给声纹层。
- 补标走「文本权威不换、只补字段」：按段序号对齐、逐段文本严格相等才写，坏文件最多丢标签
  不伤逐字稿；就地 UPDATE 不新建版本，以段 id 为主键的语义向量与全文索引零影响。
- 手改保护用确定性信号（编辑必产草稿版本，一条 SQL）替代内容 hash 启发式；speakers 表同步
  固定 UPSERT 保名语义；回填幂等靠带脚本版本号的事件台账，不靠「有没有数据」。
- 并发控制复用扫描器的文件锁，不靠停服（进程守护会把服务拉回来，停服窗口是幻觉）。
- 验收引入独立第二信源：库内每场标签基数对照转写侧 `.spk.txt` 的标签基数，两条独立链路
  对得上才算数——切块 bug 正是这条验收抓出来的。
- 新增功能文档：`docs/speaker-diarization.md`（七条设计决策 + 四角色带信息差评审方法）。
- 新增参考实现：`speaker-diarization/chunk_aware_labels.py`（切块感知标签解析）、
  `speaker-diarization/inplace_backfill.py`（就地补标骨架：对齐校验 / UPSERT / 事件幂等）。

**关键设计决策**：解析层的职责是保真不是合并；改动方案好坏看「血缘半径」不看「改没改已有行」；
有确定性标记时不用启发式比对；幂等看事件台账不看数据本身。详见新文档。

## v1.2（2026-08-15）

**通知三次触达与瘦身备份**

- 一场会的通知从「转写成功」一条，拆成「转写完成 → 纪要写好 → 任务待确认」三次触达，每条回答
  发生了什么、要不要我动手、动手去哪。
- 通知落幂等台账（`kind + ref_key`），同一份纪要、同一批任务只发一次；失败只记日志，靠扫描周期自然补发。
- 纪要写好卡把纪要压成摘要 + 决议 + 下一步，抽不到就留空，兼容两套纪要模板，决议与议题措辞分开。
- 备份剥离可从源数据重建的派生表（语义索引向量），单份备份 407M → 118M；剥失败保留完整副本。
- 修复参考实现里任务确认卡「驳回」按钮点不动的缺陷（回调空 body 不带 Content-Type 被服务端挡回）。
- 修复参考实现里回调监听进程 CSRF token 失效导致的「卡片点了没反应」（403 自动重新握手），并让
  写失败显性化（发一条失败提示，不再静默重绘）。
- 新增参考实现：`task-notify/minutes_card.py`（纪要卡构造）。
- 新增功能文档：`docs/notification-triple-touch.md`、`docs/backup-derived-table-strip.md`。

**关键设计决策**：通知幂等用「同一份内容的标识」做 ref_key（纪要按版本、任务按批次）；派生表剥离的
安全判据是「重建入口在服务启动后自动跑」；空 body 不等于没有 body。详见两份新文档。

## v1.1（2026-08-04）

**任务通知推送与确认闭环**

- 纪要完成后，本地 AI 从会议内容提取任务草稿（带来源、建议执行方、会上原话、时间锚点）。
- 任务先进「待确认」闸门，不确认永远不进正式清单——把会上拍板与闲聊分开。
- 推送飞书「会后任务确认」卡片：每条任务独立的「✅确认 / 驳回」按钮，逐条确认，不做批量。
- 确认/驳回经长连接回调回到本机，任务状态落库，卡片原地更新。
- 卡片自包含：确认后保留来源/建议/原话，可追溯；不产任何外链（本地化部署，外部网络访问不到）。
- 项目看板同步任务状态，支持执行、登记交付物、标记完成。
- 新增参考实现：`task-notify/cards.py`（卡片构造）、`task-notify/card_listener.py`（长连接回调监听）。
- 新增功能文档：`docs/task-notification-push.md`；README 补「1.1 任务闭环」「历史与设计理念」。

**关键设计决策**：逐条确认而非批量；卡片带原话辅助决策；确认后保留细节可追溯；回调走长连接零公网暴露；
任务提取先进待确认闸门。完整取舍见 [docs/task-notification-push.md](docs/task-notification-push.md)。

## v1.0（2026-07-30）

- 本地会议录音工作流：录音落地即自动转写、归档、建立索引。
- 三类本地模型（FunASR / Whisper / bge-small-zh），音频与逐字稿不出本机。
- 纪要生成派给本地配置的 Agent（Claude Code / Codex）。
- 工作台支持资料库、全文与语义检索、播放、逐字稿编辑、「复制文件夹路径」给 Agent。
